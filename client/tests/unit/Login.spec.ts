import { shallowMount } from '@vue/test-utils';
import Login from '@/views/Login.vue';

describe('Login should succesfully authenticate user', () => {
  it('returns an input', () => {
    const wrapper = shallowMount(Login);
  });
});
